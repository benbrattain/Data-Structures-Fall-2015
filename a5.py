memo = {}
def cookieTop(table, startr, startc):
  r = len(table)
  c = len(table[0]) #assumes that this table has consistent row like shown in class.
  if n in memo :
    return memo[n]
  elif n in table[r]  : 
    return